# torch >= 2.7 required for Blackwell GPUs (RTX 50xx, sm_120)
torch>=2.7
numpy
scipy
matplotlib
tqdm
blobfile>=1.0.5   # improved-diffusion runtime dependency
# NOTE: improved-diffusion itself is NOT pip-installable from git (its
# setup.py at the pinned commit builds an empty wheel under modern pip).
# scripts/setup_env.sh clones it fresh from OpenAI at commit 783b6740edb...
# into external/improved-diffusion and registers it via a .pth file.
